# שיעור 4 — עיצוב עמודות HTML עם CSS
## הבנה עמוקה של קישור בין HTML ל-CSS

---

## חלק א': אופנות חיבור HTML ל-CSS ⚡

### 1. **Inline Styles** — עיצוב ישיר בתוך התגית
```html
<h1 style="color: blue; font-size: 2rem;">כותרת עם עיצוב</h1>
```
⚠️ **חסרון**: קשה לתחזוקה, לא ניתן לשיתוף בדפים אחרים.

---

### 2. **Internal CSS** — עיצוב בתוך `<style>` בקובץ HTML
```html
<head>
  <style>
    h1 { color: blue; font-size: 2rem; }
  </style>
</head>
```
✅ **יתרון**: עובד לדף אחד, אבל לא משתף בדפים אחרים.

---

### 3. **External CSS** — קובץ CSS נפרד (המומלץ! ✓)
```html
<head>
  <link rel="stylesheet" href="css/style.css">
</head>
```

**בקובץ `css/style.css`:**
```css
h1 { color: blue; font-size: 2rem; }
```
✅ **יתרון**: ניתן לשיתוף בכל דפי האתר, קל לתחזוקה וערעור!

---

## חלק ב': בחירה של אלמנטים — Class ו-ID

### **סוגי בוררים (Selectors):**

#### 1. בורר אלמנט בסיסי
```css
h1 { color: red; }       /* כל ה-h1 יהיו אדומים */
```

#### 2. בורר Class `.` — משתמשים כשרוצים אותו עיצוב ליותר אלמנט
```html
<h2 class="title">כותרת 1</h2>
<h2 class="title">כותרת 2</h2>
```

```css
.title { color: blue; font-size: 1.5rem; }
```

#### 3. בורר ID `#` — משתמשים רק פעם אחת בדף
```html
<header id="main-header">כותרת הדף</header>
```

```css
#main-header { background: navy; color: white; }
```

✅ **כלל זהב**: Use CLASS for repeating elements, ID for unique elements!

---

## חלק ג': 10 משימות פשוטות — בנו HTML ועצבו עם CSS

### **משימה 1: כותרת ראשית עם רקע**
- **HTML**: `<h1>ברוכים הבאים</h1>`
- **CSS**: צבע רקע כחול, טקסט לבן, padding 20px, text-align center
```css
h1 {
  background: #001f5b;
  color: white;
  padding: 20px;
  text-align: center;
}
```

---

### **משימה 2: כפתור עם hover effect**
- **HTML**: `<button class="btn">לחץ עליי</button>`
- **CSS**: צבע רקע כחול, לחץ עליו ויהפוך לירוק
```css
.btn {
  background: #001f5b;
  color: white;
  padding: 10px 20px;
  border: none;
  border-radius: 5px;
  cursor: pointer;
  transition: background 200ms ease;
}
.btn:hover {
  background: #009900;
}
```

---

### **משימה 3: רשימה ללא נקודות**
- **HTML**: `<ul class="custom-list"><li>פריט 1</li>...</ul>`
- **CSS**: הסתר נקודות, הוסף ריווח וגבול קל
```css
.custom-list {
  list-style: none;
  padding: 0;
}
.custom-list li {
  padding: 8px;
  border-bottom: 1px solid #ccc;
}
```

---

### **משימה 4: תיבה עם צל (Card)**
- **HTML**: `<div class="card">תוכן כרטיס</div>`
- **CSS**: רקע לבן, צל עדין, border-radius
```css
.card {
  background: white;
  padding: 15px;
  border-radius: 8px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  margin: 10px 0;
}
```

---

### **משימה 5: טופס עם עיצוב**
- **HTML**: `<input type="text" class="form-input" placeholder="שם">`
- **CSS**: גבול נקי, focus effect
```css
.form-input {
  padding: 8px;
  border: 1px solid #ccc;
  border-radius: 4px;
  font-size: 1rem;
  transition: border-color 200ms;
}
.form-input:focus {
  border-color: #001f5b;
  outline: none;
}
```

---

### **משימה 6: רשת של תמונות (Grid)**
- **HTML**: `<div class="gallery"><img>...<img>...<img></div>`
- **CSS**: תמונות בשורות אוטומטיות
```css
.gallery {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
  gap: 10px;
}
.gallery img {
  width: 100%;
  height: auto;
  border-radius: 8px;
}
```

---

### **משימה 7: תפריט אופקי (Flexbox)**
- **HTML**: `<nav class="menu"><a href="#">דף בית</a>...<a>צור קשר</a></nav>`
- **CSS**: פריטים בשורה עם רווח שווה
```css
.menu {
  display: flex;
  justify-content: space-around;
  background: #001f5b;
  padding: 10px;
}
.menu a {
  color: white;
  text-decoration: none;
  padding: 8px 12px;
  transition: background 200ms;
}
.menu a:hover {
  background: rgba(255, 255, 255, 0.2);
}
```

---

### **משימה 8: בן קפיטליזציה - Capitalize Title**
- **HTML**: `<h2 class="subtitle">קטע קטן</h2>`
- **CSS**: הקשחת טקסט, גודל משתנה
```css
.subtitle {
  text-transform: uppercase;
  letter-spacing: 2px;
  color: #001f5b;
  border-bottom: 2px solid #a8d8ff;
  padding-bottom: 8px;
}
```

---

### **משימה 9: קטע טקסט עם Box Model**
- **HTML**: `<div class="text-box">טקסט עם מרווחים</div>`
- **CSS**: הדגמת padding, margin, border
```css
.text-box {
  padding: 20px;          /* רווח פנימי */
  margin: 15px 0;         /* רווח חיצוני */
  border: 2px solid #001f5b; /* גבול */
  background: #f6fbff;    /* רקע עדין */
  border-radius: 6px;
}
```

---

### **משימה 10: עמודה מרכזית (Container)**
- **HTML**: `<div class="container">תוכן</div>`
- **CSS**: רוחב מקסימלי ומרכוז בעמוד
```css
.container {
  max-width: 800px;
  margin: 0 auto;          /* מרכוז אופקי */
  padding: 20px;
  background: white;
}
```

---

## חלק ד': הסבר 20 תכונות CSS בסיסיות

| תכונה | הסבר | דוגמה |
|-------|------|--------|
| `color` | צבע הטקסט | `color: #001f5b;` |
| `background` | צבע רקע | `background: white;` |
| `padding` | רווח פנימי | `padding: 20px;` |
| `margin` | רווח חיצוני | `margin: 15px;` |
| `border` | גבול מסביב | `border: 2px solid red;` |
| `border-radius` | עיגול פינות | `border-radius: 8px;` |
| `font-size` | גודל גופן | `font-size: 1.5rem;` |
| `font-weight` | עובי הטקסט | `font-weight: 700;` |
| `text-align` | יישור טקסט | `text-align: center;` |
| `width` | רוחב אלמנט | `width: 100%;` |
| `height` | גובה אלמנט | `height: 200px;` |
| `display` | סוג התצוגה | `display: flex;` |
| `flex-direction` | כיוון Flex | `flex-direction: column;` |
| `justify-content` | יישור בציר ראשי | `justify-content: space-around;` |
| `align-items` | יישור בציר משני | `align-items: center;` |
| `gap` | רווח בין פריטים | `gap: 10px;` |
| `box-shadow` | צל מסביב | `box-shadow: 0 4px 8px rgba(0,0,0,0.1);` |
| `transition` | אנימציה | `transition: background 200ms;` |
| `text-decoration` | קו על טקסט | `text-decoration: none;` |
| `cursor` | סוג הסמן | `cursor: pointer;` |

---

## חלק ה': דוגמאות מעשיות

### **דוגמה 1: כרטיס ידידות (Card)**
ראה בקובץ `דוגמה1_כרטיס.html` ו-`דוגמה1_כרטיס.css`

### **דוגמה 2: תפריט אפליקציה (Menu)**
ראה בקובץ `דוגמה2_תפריט.html` ו-`דוגמה2_תפריט.css`

---

## חלק ו': טיפים למעבר מ-HTML ל-CSS

### **כלל 1: Separation of Concerns (הפרדת תפקידים)**
- **HTML** = מבנה ותוכן
- **CSS** = עיצוב ונראות
- **JavaScript** = אינטרקטיביות (בשיעור הבא)

### **כלל 2: Reusability (שימוש חוזר)**
```html
<h2 class="section-title">כותרת 1</h2>
<h2 class="section-title">כותרת 2</h2>
```
↓
```css
.section-title { /* עיצוב מיוחד */ }
```

### **כלל 3: Specificity (דיוק בבחירה)**
- ID הוא היותר ספציפי (`#header` — משתמשו רק כאשר חייב)
- Class בינוני (`.button` — מומלץ!)
- אלמנט בסיסי הוא הכללי ביותר (`button` — לעיצובים כלליים)

### **כלל 4: Cascade (זרימה של סגנונות)**
```css
/* סגנון כללי */
button { background: blue; }

/* סגנון ספציפי יותר דורס את הכללי */
.btn-primary { background: red; }

/* וכן הלאה */
.btn-primary:hover { background: darkred; }
```

---

## חלק ז': משימות למעבדה — תרגול עמוק

### **תרגול 1: שנו את הצבעים**
קחו דוגמה 1, שנו את קבוע הצבע מכחול (`#001f5b`) לירוק (`#006600`) בקובץ CSS. צפו בשינוי בכל האלמנטים.

### **תרגול 2: הוסיפו כפתור חדש**
בקובץ HTML, הוסיפו `<button class="btn btn-secondary">כפתור משני</button>` וצרו CSS עבור `.btn-secondary` בעיצוב שונה.

### **תרגול 3: שנו את הרוחב והמיקום**
שנו את `max-width` ב-`.container` ובדקו כיצד האתר משתנה בחלון קטן וגדול.

### **תרגול 4: Responsive Design (עיצוב תגובתי)**
בתוך ה-CSS, הוסיפו:
```css
@media (max-width: 768px) {
  .gallery {
    grid-template-columns: repeat(2, 1fr);
  }
}
```
וראו כיצד הגלריה משתנה בטלפון.

### **תרגול 5: צרו רשימת קניות**
בנו קובץ HTML חדש עם `<ul><li>פריט 1</li>...</ul>`, עצבו אותה עם CSS, וסימנו פריטים שלא ניתן לקנות בצבע שונה.

---

## סיכום היעדים 🎯

בסוף שיעור זה, תלמיד צריך להכיר:

1. ✅ **שלוש דרכים לחיבור CSS ל-HTML** (Inline, Internal, External)
2. ✅ **בחירה של אלמנטים עם Class ו-ID**
3. ✅ **Box Model** — Padding, Margin, Border
4. ✅ **Flexbox** — סידור אופקי וגמיש
5. ✅ **Grid** — רשתות של אלמנטים
6. ✅ **Hover Effects** — שינויים בלחיצה
7. ✅ **Responsive Design** — עיצוב לטלפון וקומפיוטר
8. ✅ **ההפרדה בין HTML ל-CSS** — כיוון העבודה המעשית

---

## משימה סופית 💪

**בנו אתר ביתי שלכם עם:**
- כותרת ראשית
- תפריט ניווט
- 3 כרטיסים (cards) עם תמונות
- טופס קשר בתחתית

**דרישות:**
- HTML חופשי מעיצוב (Class ו-ID בלבד)
- CSS מרכזי בקובץ נפרד
- לפחות 3 צבעים שונים
- 2 hover effects

בהצלחה! 🚀

